# Исправление проблемы: Worker засыпает ночью

## 🔴 Проблема

Worker на Render.com засыпает после периода бездействия (idle timeout ~15 минут). Когда Worker спит, он не может получать сообщения из Telegram каналов.

## ✅ Решения

### Решение 1: Настройка автоматического пробуждения через cron-job.org (РЕКОМЕНДУЕТСЯ)

**Шаги:**

1. **Зарегистрируйтесь на [cron-job.org](https://cron-job.org)** (бесплатно)

2. **Создайте новую задачу:**
   - Нажмите "Create cronjob"
   - **Title**: "Пробуждение Worker каждые 5 минут"
   - **Address (URL)**: `https://chatbot-tg.vercel.app/api/cron/wake-worker`
   - **Schedule**: Выберите "Every 5 minutes" или "Every 10 minutes"
   - **Request Method**: GET (или POST)
   - **Save**

3. **Альтернатива (прямой вызов Worker):**
   - **Address (URL)**: `https://chatbot-tg.onrender.com/runner/wake`
   - **Request Method**: ⚠️ **POST** (обязательно!)
   - **Schedule**: Каждые 5-10 минут

**Преимущества:**
- ✅ Работает автоматически 24/7
- ✅ Бесплатно
- ✅ Надежно
- ✅ Не требует изменений в коде

### Решение 2: Использование Vercel Cron Jobs (если доступно)

Если у вас есть доступ к Vercel Cron Jobs:

1. Создайте файл `vercel.json` в корне проекта:
```json
{
  "crons": [
    {
      "path": "/api/cron/wake-worker",
      "schedule": "*/5 * * * *"
    }
  ]
}
```

2. Задеплойте изменения на Vercel

**Примечание:** Vercel Cron Jobs доступны только на платных планах.

### Решение 3: Ручное пробуждение (временное решение)

Если Worker уснул, можно пробудить его вручную:

```bash
# Вариант 1: Через Vercel endpoint
curl https://chatbot-tg.vercel.app/api/cron/wake-worker

# Вариант 2: Прямо через Worker
curl -X POST https://chatbot-tg.onrender.com/runner/wake

# Вариант 3: Через Vercel worker endpoint
curl https://chatbot-tg.vercel.app/api/worker/wake
```

## 🔍 Как проверить, что Worker работает

### Проверка статуса Worker:

```bash
curl https://chatbot-tg.onrender.com/runner/status
```

Должен вернуть:
```json
{
  "isRunning": true,
  "monitoring": {
    "isMonitoring": true,
    "isConnected": true
  }
}
```

### Проверка логов:

1. **Логи Worker на Render.com:**
   - Откройте Render.com Dashboard → Logs
   - Ищите: `🔔 Запрос на пробуждение Worker...`
   - Должны появляться каждые 5-10 минут

2. **Логи Vercel:**
   - Откройте Vercel Dashboard → Logs
   - Ищите: `[CRON] Запрос на пробуждение Worker...`

## 🎯 Ожидаемое поведение

После настройки cron:

1. **Каждые 5-10 минут** cron отправляет запрос на пробуждение
2. Worker проверяет свой статус
3. Если Worker уснул, он пробуждается автоматически
4. Мониторинг каналов перезапускается
5. Worker остается активным и может получать сообщения

## ⚠️ Важные замечания

1. **Не запускайте локальные скрипты**, которые используют `TELEGRAM_SESSION_STRING`, пока Worker работает на Render.com
   - Это вызывает ошибку `AUTH_KEY_DUPLICATED`
   - Worker не сможет подключиться к Telegram

2. **Проверяйте логи регулярно** - если cron не работает, Worker будет засыпать

3. **На Render.com бесплатный план** имеет idle timeout ~15 минут
   - Если нет активности, сервис засыпает
   - Cron запросы предотвращают это

## 💡 Дополнительные решения

### Если cron-job.org не работает:

1. **Используйте другой cron сервис:**
   - [EasyCron](https://www.easycron.com/)
   - [UptimeRobot](https://uptimerobot.com/) (мониторинг + keep-alive)
   - [Healthchecks.io](https://healthchecks.io/)

2. **Используйте платный план Render.com:**
   - На платном плане нет idle timeout
   - Worker работает постоянно

3. **Используйте другой хостинг:**
   - Railway.app - нет idle timeout на free плане
   - Fly.io - нет idle timeout
   - DigitalOcean App Platform - более длительный idle timeout

## 📋 Чеклист настройки

- [ ] Зарегистрировались на cron-job.org
- [ ] Создали задачу с правильным URL
- [ ] Установили частоту: каждые 5-10 минут
- [ ] Проверили, что задача активна
- [ ] Проверили Execution History в cron-job.org
- [ ] Проверили логи Worker на Render.com
- [ ] Проверили статус Worker: `curl https://chatbot-tg.onrender.com/runner/status`

## 🚀 Быстрый старт

1. **Пробудите Worker сейчас:**
   ```bash
   curl -X POST https://chatbot-tg.onrender.com/runner/wake
   ```

2. **Настройте cron-job.org:**
   - URL: `https://chatbot-tg.vercel.app/api/cron/wake-worker`
   - Частота: каждые 5 минут
   - Метод: GET

3. **Проверьте через 10 минут:**
   - Логи Worker должны показывать запросы каждые 5 минут
   - Статус Worker должен быть `isRunning: true`


