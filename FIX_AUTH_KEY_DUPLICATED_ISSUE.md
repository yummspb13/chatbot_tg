# Исправление ошибки AUTH_KEY_DUPLICATED

## 🔴 Проблема

Ошибка `406: AUTH_KEY_DUPLICATED` означает, что `TELEGRAM_SESSION_STRING` используется одновременно в нескольких местах. Это блокирует подключение Worker к Telegram, поэтому бот не получает сообщения из каналов через Client API.

## 🔍 Причины

1. **Локальный Worker запущен** и использует ту же сессию, что и Worker на Render.com
2. **Несколько инстансов Worker** на Render.com используют одну сессию
3. **Сессия используется** и в основном приложении, и в Worker одновременно

## ✅ Решения

### Решение 1: Остановить локальный Worker (если запущен)

```bash
# Проверьте, запущен ли локальный Worker
ps aux | grep "worker"

# Если запущен, остановите его
# Нажмите Ctrl+C в терминале, где запущен Worker
# Или найдите процесс и убейте его:
kill <PID>
```

### Решение 2: Убедиться, что сессия используется только в одном месте

**Важно:** `TELEGRAM_SESSION_STRING` должен использоваться **только в Worker на Render.com**, а не в основном приложении.

**Проверьте:**
1. В основном приложении (Vercel) **НЕ должно быть** `TELEGRAM_SESSION_STRING`
2. В Worker (Render.com) **должен быть** `TELEGRAM_SESSION_STRING`

### Решение 3: Создать отдельную сессию для Worker (если нужно)

Если вам нужна сессия и в основном приложении, и в Worker, создайте **две разные сессии**:

1. **Сессия 1** - для основного приложения (если нужно)
2. **Сессия 2** - для Worker (обязательно)

**Как создать новую сессию:**
```bash
# Запустите скрипт создания сессии
npm run client:setup

# Или через админ-панель:
# https://your-app.vercel.app/admin/qr-auth
```

## 🔧 Пошаговая инструкция

### Шаг 1: Проверьте, где используется сессия

1. **Vercel (основное приложение):**
   - Откройте Vercel Dashboard → Settings → Environment Variables
   - Проверьте, есть ли `TELEGRAM_SESSION_STRING`
   - **Если есть** - удалите его (он не нужен в основном приложении)

2. **Render.com (Worker):**
   - Откройте Render.com Dashboard → Environment
   - Проверьте, что `TELEGRAM_SESSION_STRING` установлен
   - **Если нет** - добавьте его

### Шаг 2: Остановите локальный Worker (если запущен)

```bash
# Проверьте запущенные процессы
ps aux | grep "worker"

# Остановите локальный Worker
# (нажмите Ctrl+C в терминале, где он запущен)
```

### Шаг 3: Перезапустите Worker на Render.com

```bash
# Остановите Worker
curl -X POST https://chatbot-tg.onrender.com/runner/stop

# Подождите 5 секунд
sleep 5

# Запустите Worker
curl -X POST https://chatbot-tg.onrender.com/runner/start
```

### Шаг 4: Проверьте подписки

```bash
# После перезапуска Worker, проверьте подписки
npm run check:channel:subscriptions
```

Если ошибка `AUTH_KEY_DUPLICATED` исчезла, значит проблема решена.

## 🎯 Ожидаемый результат

После исправления:
- ✅ Worker успешно подключается к Telegram
- ✅ Worker получает сообщения из всех каналов
- ✅ Бот обрабатывает сообщения из всех каналов
- ✅ Ошибка `AUTH_KEY_DUPLICATED` не появляется

## 💡 Дополнительная информация

### Почему это важно

Worker использует Telegram Client API для получения сообщений из каналов, где бот не является администратором. Если Worker не может подключиться из-за `AUTH_KEY_DUPLICATED`, он не получает сообщения, и бот читает только каналы, где он админ.

### Как избежать проблемы в будущем

1. **Используйте одну сессию только в одном месте**
2. **Не запускайте локальный Worker**, если Worker уже работает на Render.com
3. **Проверяйте логи Worker** на наличие ошибок подключения

