# 🔍 Чек-лист диагностики

## ✅ Проверьте по порядку:

### 1. Бот запущен?

**Проверка:**
```
/status
```

**Должно быть:**
- `isRunning: true`
- `mode: MANUAL` или `AUTO`

**Если нет:**
```
/start
```

---

### 2. Город и канал добавлены?

**Проверка:**
```
/listchannels
```

**Должно показать:**
- ✅ Город: Москва (moskva)
- ✅ Канал: Название (Chat ID)

**Если нет:**
```
/addcity Москва
/addchannel moskva @ваш_канал "Название"
```

---

### 3. Бот - администратор канала/группы?

**Проверка:**
1. Откройте канал/группу в Telegram
2. Настройки → Администраторы
3. Убедитесь, что бот есть в списке

**Если нет:**
- Добавьте бота как администратора
- Дайте права на чтение сообщений

---

### 4. Проверьте логи бота

**Что делать:**
1. Откройте терминал, где запущен `npm run bot:polling`
2. Отправьте сообщение в канал/группу
3. Смотрите логи

**Что должно появиться:**
```
🔵 handleChannelMessage ВЫЗВАН
   Chat Type: channel (или group/supergroup)
   Chat ID: -1001234567890
   ✅ Канал найден: "Название"
   ✅ Бот запущен
```

**Если НЕ появляется:**
- Бот не получает обновления
- Проверьте, что бот - администратор
- Проверьте Chat ID

---

### 5. Проверьте Chat ID

**Проблема:** Chat ID может быть неправильным

**Решение:**
1. Используйте бота @userinfobot
2. Или проверьте через `/listchannels` - там показан Chat ID
3. Убедитесь, что Chat ID в базе совпадает с реальным

**Для групп:**
- Обычная группа: `-123456789`
- Супергруппа: `-1001234567890` (начинается с `-100`)

---

### 6. Проверьте базу данных

**Что проверить:**
```bash
npx prisma studio
```

Откройте таблицы:
- `City` - должен быть ваш город
- `Channel` - должен быть ваш канал
  - `isActive: true`
  - `chatId` правильный

---

## 🐛 Типичные проблемы:

### Проблема 1: Логи не появляются

**Причина:** Бот не получает обновления

**Решение:**
1. Бот должен быть администратором
2. Перезапустите бота (`Ctrl+C`, затем `npm run bot:polling`)
3. Проверьте, что бот запущен (`/status`)

### Проблема 2: "Канал не найден в базе"

**Причина:** Chat ID не совпадает

**Решение:**
1. Проверьте Chat ID через `/listchannels`
2. Убедитесь, что Chat ID правильный
3. Пересоздайте канал через `/addchannel`

### Проблема 3: "Бот не запущен"

**Причина:** Бот остановлен

**Решение:**
```
/start
```

---

## 📋 Быстрая проверка:

1. ✅ `/status` → `isRunning: true`
2. ✅ `/listchannels` → видно город и канал
3. ✅ Бот - администратор канала/группы
4. ✅ Отправьте сообщение → проверьте логи
5. ✅ Должно появиться: `🔵 handleChannelMessage ВЫЗВАН`

---

## 💡 Что делать сейчас:

1. **Проверьте логи бота:**
   - Откройте терминал с `npm run bot:polling`
   - Отправьте сообщение в канал/группу
   - Что появляется в логах?

2. **Проверьте статус:**
   ```
   /status
   ```

3. **Проверьте каналы:**
   ```
   /listchannels
   ```

4. **Проверьте, что бот - администратор:**
   - Откройте канал/группу
   - Настройки → Администраторы
   - Бот должен быть в списке

---

## 🔧 Если ничего не помогает:

**Попробуйте:**
1. Перезапустите бота (`Ctrl+C`, затем `npm run bot:polling`)
2. Удалите и пересоздайте канал:
   ```
   /removechannel <id>
   /addchannel moskva @канал "Название"
   ```
3. Проверьте базу данных через `npx prisma studio`

