import { Markup } from 'telegraf'

/**
 * Создает клавиатуру с основными командами бота
 */
export function getMainKeyboard() {
  return Markup.keyboard([
    ['📊 Статус', '▶️ Запустить', '⏹ Остановить'],
    ['🏙️ Города', '📡 Каналы'],
    ['⚙️ Настройки', '🎓 Обучение'],
    ['📝 Черновики', '📋 Список каналов'],
  ])
    .resize()
    .persistent()
}

/**
 * Создает компактную клавиатуру (меньше кнопок)
 */
export function getCompactKeyboard() {
  return Markup.keyboard([
    ['📊 Статус', '▶️ Запустить', '⏹ Остановить'],
    ['📡 Каналы', '⚙️ Настройки'],
  ])
    .resize()
    .persistent()
}

/**
 * Убирает клавиатуру
 */
export function removeKeyboard() {
  return Markup.removeKeyboard()
}

