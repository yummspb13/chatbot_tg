# Быстрое исправление проблемы с worker

## Проблема
Worker не читает сообщения из каналов, логи отсутствуют.

## Решение за 5 минут

### Шаг 1: Проверьте статус worker

```bash
curl https://chatbot-tg.onrender.com/runner/status
```

Если `isRunning: false` или `isMonitoring: false` - запустите:

```bash
curl -X POST https://chatbot-tg.onrender.com/runner/start
```

### Шаг 2: Проверьте переменные окружения в Render.com

**ОБЯЗАТЕЛЬНО должны быть установлены:**

1. **TELEGRAM_SESSION_STRING** - строка сессии (из QR-логина)
2. **TELEGRAM_API_ID** - ID приложения
3. **TELEGRAM_API_HASH** - Hash приложения
4. **MAIN_APP_URL** - URL вашего Vercel приложения: `https://chatbot-tg.vercel.app`
5. **BOT_API_KEY** - должен совпадать с `BOT_API_KEY` в Vercel
6. **BOT_WEBHOOK_URL** - тот же URL что и `MAIN_APP_URL`

### Шаг 3: Проверьте, что каналы есть в базе

Проверьте через API:

```bash
curl -H "Authorization: Bearer YOUR_BOT_API_KEY" https://chatbot-tg.vercel.app/api/channels
```

Должен вернуть список каналов.

### Шаг 4: Проверьте, что аккаунт подписан на канал

**ВАЖНО:** Аккаунт `@yummspb` (7007868967) должен быть подписан на канал, который вы мониторите.

### Шаг 5: Альтернатива - используйте MONITOR_CHANNELS

Если API не работает, установите в Render.com переменную:

**MONITOR_CHANNELS** = 
```json
[{"chatId": "-1001234567890", "title": "Название канала"}]
```

Замените `-1001234567890` на реальный chatId вашего канала.

## Быстрая диагностика

1. Откройте Render.com Dashboard → Ваш worker → Logs
2. Запустите worker: `curl -X POST https://chatbot-tg.onrender.com/runner/start`
3. Проверьте логи - должны появиться:
   - `✅ Подключен к Telegram через Client API`
   - `📡 Начинаю мониторинг X каналов...`
   - Список каналов

Если этих логов нет - проблема в переменных окружения или подключении.

## Если ничего не помогает

Используйте простой вариант - установите `MONITOR_CHANNELS` в Render.com с вашим каналом и перезапустите worker.

