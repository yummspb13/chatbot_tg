# ⚠️ Воркер для мониторинга каналов конкурентов

## 🔍 Текущая ситуация:

**Проблема:** Бот не может быть администратором каналов конкурентов, поэтому не получает обновления напрямую.

**Решение:** Использовать воркер с Client API (аккаунт @yummspb) для мониторинга каналов и автоматической пересылки сообщений в бота.

---

## ✅ Временное решение (для тестирования):

### Ручная пересылка сообщений

1. Откройте канал конкурента
2. Перешлите сообщение в бота (в личку боту)
3. Бот обработает пересланное сообщение и создаст событие

**Как это работает:**
- Бот обрабатывает пересланные сообщения из каналов
- Код уже поддерживает это в `webhook-handlers.ts`
- Это работает для тестирования

---

## 🚀 Постоянное решение (нужно реализовать):

### Воркер с автоматическим мониторингом

**Что нужно сделать:**

1. **Реализовать мониторинг в воркере:**
   - Воркер подключается к Telegram через Client API (аккаунт @yummspb)
   - Воркер читает сообщения из каналов конкурентов
   - Воркер автоматически пересылает сообщения в бота

2. **Интеграция с базой данных:**
   - Воркер должен получать список каналов из базы
   - Воркер должен мониторить только активные каналы

3. **API для управления:**
   - `POST /runner/start` - запустить мониторинг
   - `POST /runner/stop` - остановить мониторинг
   - `GET /runner/status` - статус мониторинга

---

## 📋 Что уже есть:

✅ Воркер развернут на Render.com
✅ Сессия добавлена в Environment Variables
✅ QR-авторизация работает
✅ Базовая структура воркера готова

❌ Мониторинг каналов еще не реализован (TODO в коде)

---

## 🔧 Что нужно реализовать:

### 1. В `worker/src/routes/runner.ts`:

```typescript
import { startChannelMonitoringSimple } from '@/lib/telegram/client-api-simple'

router.post('/start', async (req, res) => {
  // ...
  await startChannelMonitoringSimple()
  // ...
})
```

### 2. Воркер должен:
- Подключиться к Telegram через Client API
- Получить список каналов из базы данных
- Подписаться на обновления из этих каналов
- Автоматически пересылать сообщения в бота

### 3. Бот должен:
- Обрабатывать пересланные сообщения (уже работает)
- Создавать события из пересланных сообщений

---

## 💡 Пока что:

**Используйте ручную пересылку для тестирования:**
1. Откройте канал конкурента
2. Перешлите сообщение в бота
3. Бот обработает и создаст событие

**После реализации воркера:**
- Воркер будет автоматически пересылать все новые сообщения
- Бот будет обрабатывать их автоматически

---

## 🎯 Следующие шаги:

1. ✅ Протестируйте ручную пересылку
2. ⏳ Реализуйте мониторинг в воркере
3. ⏳ Настройте автоматическую пересылку

