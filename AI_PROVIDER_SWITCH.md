# 🔄 Переключение AI Провайдеров

Быстрое переключение между AI провайдерами для разных окружений.

## 📋 Доступные провайдеры

### 1. **MOCK** (локальное тестирование)
- ✅ Работает без API вызовов
- ✅ Не требует пополнения баланса
- ✅ Быстрые ответы для разработки
- ✅ Подходит для локального тестирования

### 2. **OpenAI** (продакшен)
- ✅ Высокое качество ответов
- ✅ Требует `OPENAI_API_KEY`
- ✅ Может быть недоступен в РФ

### 3. **DeepSeek** (альтернатива)
- ✅ Работает в РФ
- ✅ Требует `DEEPSEEK_API_KEY`
- ✅ Требует пополнения баланса

## 🚀 Быстрое переключение

### Переключить на MOCK (локальное тестирование):
```bash
npm run ai:switch mock
```

### Переключить на OpenAI:
```bash
npm run ai:switch openai
```

### Переключить на DeepSeek:
```bash
npm run ai:switch deepseek
```

### Проверить текущий провайдер:
```bash
npm run ai:status
```

## 📝 Ручное переключение

Или просто измените в `.env`:
```env
AI_PROVIDER=mock      # для локального тестирования
AI_PROVIDER=openai   # для OpenAI
AI_PROVIDER=deepseek # для DeepSeek
```

## 🔧 Настройка для каждого провайдера

### MOCK
```env
AI_PROVIDER=mock
# API ключи не требуются
```

### OpenAI
```env
AI_PROVIDER=openai
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini  # опционально
```

### DeepSeek
```env
AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_MODEL=deepseek-chat  # опционально
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1  # опционально
```

## 💡 Рекомендации

### Для локальной разработки:
```bash
npm run ai:switch mock
npm run dev
```

### Для продакшена (Vercel):
1. Установите переменные окружения в Vercel:
   - `AI_PROVIDER=openai`
   - `OPENAI_API_KEY=sk-...`

2. Или используйте DeepSeek:
   - `AI_PROVIDER=deepseek`
   - `DEEPSEEK_API_KEY=sk-...`

## ⚠️ Важно

После переключения провайдера **перезапустите приложение**:
```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова:
npm run dev
```

## 🔍 Проверка текущего провайдера

В логах при запуске вы увидите:
```
🤖 AI Provider: MOCK (локальное тестирование)
🤖 AI Provider: OPENAI
🤖 AI Provider: DEEPSEEK
```

