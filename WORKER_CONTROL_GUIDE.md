# 🔧 Управление Worker

## Где находится переключатель?

Переключатель Worker находится в **Telegram боте** через команды:

### Команды для управления Worker:

1. **`/worker`** - показать текущий статус Worker
2. **`/worker on`** - включить Worker
3. **`/worker off`** - отключить Worker

### Как использовать:

1. Откройте Telegram бота
2. Напишите команду `/worker` - увидите текущий статус
3. Напишите `/worker off` - отключить Worker
4. Напишите `/worker on` - включить Worker обратно

## Текущее состояние

**По умолчанию Worker ВКЛЮЧЕН** (`workerEnabled: true`)

Это видно в коде:
- `lib/telegram/bot.ts` - при создании настроек по умолчанию `workerEnabled: true`
- `prisma/schema.prisma` - в схеме `workerEnabled Boolean @default(true)`

## Где проверяется настройка Worker?

Настройка `workerEnabled` проверяется в следующих местах:

1. **`lib/worker/wake-worker.ts`** - при пробуждении Worker
   - Если `workerEnabled = false`, Worker не пробуждается

2. **`app/api/tg/webhook/route.ts`** - при получении сообщений из каналов
   - Если `workerEnabled = false`, не пытается пробудить Worker

3. **`lib/telegram/commands.ts`** - команда `/status`
   - Показывает статус Worker в общем статусе бота

## Что делать дальше?

### 1. Проверьте текущий статус Worker:

Отправьте боту команду:
```
/worker
```

Вы увидите:
- Текущий статус (Включен/Отключен)
- Инструкции по управлению

### 2. Если хотите отключить Worker:

Отправьте:
```
/worker off
```

После этого:
- ✅ Бот будет работать с сообщениями, отправленными напрямую боту
- ✅ Каналы, где бот является администратором, обрабатываются автоматически
- ❌ Каналы конкурентов не мониторятся (Worker не работает)

### 3. Если хотите включить Worker обратно:

Отправьте:
```
/worker on
```

После этого:
- ✅ Автоматический мониторинг всех каналов через Client API
- ✅ Работает даже для каналов, где бот не является администратором

### 4. Проверьте общий статус бота:

Отправьте:
```
/status
```

Вы увидите:
- Статус бота (Работает/Остановлен)
- Режим (Автоматический/Ручной)
- **Статус Worker** (Включен/Отключен)
- Порог уверенности
- Количество активных каналов
- Статистику обучения

## Важно!

- **Worker отключен** = экономия ресурсов, работа только через прямую отправку
- **Worker включен** = автоматический мониторинг всех каналов (нужен Worker на Render.com)

Вы можете переключать Worker в любой момент без перезапуска бота!

