# Исправление MAIN_APP_URL в Worker

## Проблема

В Worker `MAIN_APP_URL` установлен как `chatbot-tg.vercel.app` (без `https://`), что может вызывать проблемы при отправке запросов.

## Решение

### В Render.com Dashboard:

1. Откройте https://dashboard.render.com
2. Выберите ваш Web Service (Worker)
3. Environment
4. Найдите переменную `MAIN_APP_URL`
5. Измените значение на: `https://chatbot-tg.vercel.app`
6. Сохраните изменения
7. Render автоматически перезапустит сервис

### Проверка после исправления:

```bash
npm run check:worker:status
```

Должно показать:
```
MAIN_APP_URL: ✅
   Значение: https://chatbot-tg.vercel.app
```

## Дополнительная диагностика

После исправления `MAIN_APP_URL`:

1. Отправьте тестовое сообщение в канал
2. Проверьте логи Worker в Render.com Dashboard
3. Должно появиться:
   ```
   📨 Получено сообщение из канала ...
   🔄 Отправляю сообщение боту на https://chatbot-tg.vercel.app/api/tg/webhook...
   ✅ Сообщение отправлено боту через webhook
   ```

4. Проверьте логи Vercel
5. Должно появиться:
   ```
   📥 WEBHOOK RECEIVED: message
   📨 MESSAGE: ...
   ✅ WEBHOOK: processed
   ```

## Если проблемы остаются

Проверьте:
1. Каналы добавлены в базу данных и `isActive = true`
2. Аккаунт (который используется в `TELEGRAM_SESSION_STRING`) подписан на каналы
3. `chatId` в базе данных совпадает с реальным ID канала

