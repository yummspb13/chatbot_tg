# Polymarket 5m — fair-модель (M5), 18.08.2026

## Вердикт: REJECTED (лицензии нет — мейкер будет котировать без скоса)

Логистическая регрессия P(up) следующей 5-минутки BTC по микроструктуре минуток.
Датасет: **1877 бакетов** 11-18.08 (харвест M4 × минутки BTC-USD Dukascopy;
139 бакетов выпали из-за неполных минуток — паузы фида на выходных).
Walk-forward: train 1315 (11-15.08), test 562 (16-18.08). Фичи на момент t0,
без заглядывания: ret1, mom3, mom5 (нормированы на волу), drift5 (ход прошлого
бакета), volRatio (лог-отношение к медианной воле трейна), prevOut.

## Батч гипотез (зафиксирован до прогона)

3 конфигурации λ ∈ {0.1, 1, 10}; выбор по log-loss на хвосте трейна (20%),
тест тронут один раз финальной. Выбрана λ=10 (сильная регуляризация —
ожидаемо для слабого сигнала).

## Результаты (тест, 562 бакета)

| метрика | модель | база (константа p̂=0.512) |
|---|---|---|
| log-loss | **0.68955** | 0.69260 |
| accuracy | **54.6%** | 50.5% |

Значимость: t(paired log-loss) = **1.05**, z(accuracy) = **1.96**.
Кумулятивный порог леджера Бонферрони: z ≥ **4.83** (N = 51 242 гипотезы).
**Не пройден с большим запасом → rejected.**

Осторожно с соблазном: 54.6% и z≈2 «на глаз» выглядят как край, но при
51 тысяче протестированных гипотез такие значения регулярно возникают из шума —
ровно от этого защищает кумулятивный порог. Подтверждение M4: исходы — монетка
(UP 51.0%, автокорреляции нет).

## Веса (стандартизованные фичи)

ret1 **+0.122** · mom5 +0.091 · drift5 −0.078 · volRatio +0.033 · mom3 −0.005 · prevOut −0.007 · bias +0.020

Читается как слабый минутный моментум с внутрибакетным реверсом — направление
любопытное, но не доказанное; вернёмся к нему, только если форвардные данные
коллектора (не история!) независимо покажут то же.

## Оговорки

- Таргет резолвится по Chainlink/Pyth TWAP-60с, фичи — по CFD-котировке
  Dukascopy: расхождение источников — дополнительный шум таргета (не утечка).
- 7 дней истории — минимум; модель можно переучить на месяце данных, но батч
  снова пойдёт в леджер, порог только вырастет.

## Следствия для лупа

- Мейкер M6 (если гейт окон M4 пройдёт по глубине) стартует **симметричным**:
  'set-arb' и 'maker-sym'; ростер 'maker-fair' остаётся выключенным.
- Дашборд: Neural Shell показывает фактические веса и честную плашку REJECTED.
- Леджер: totalTested 51 239 → **51 242** (+3), stage1 0.
