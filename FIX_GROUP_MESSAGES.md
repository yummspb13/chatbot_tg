# Исправление: Сообщения из группы не обрабатываются

## ❌ Проблема:
Публикуете сообщение в тестовой группе, но ничего не происходит.

## 🔍 Возможные причины:

### 1. Бот не является администратором группы

**Важно:** Telegram Bot API позволяет получать обновления из каналов/групп **ТОЛЬКО** если бот является администратором!

**Решение:**
1. Откройте группу в Telegram
2. Перейдите в **Настройки группы** → **Администраторы**
3. Добавьте вашего бота как администратора
4. Дайте боту права на чтение сообщений

### 2. Группа не добавлена в базу данных

Бот обрабатывает только те каналы/группы, которые добавлены через `/addchannel`.

**Решение:**
1. Получите Chat ID группы:
   - Добавьте бота в группу
   - Отправьте любое сообщение в группу
   - Используйте бота @userinfobot для получения Chat ID
   - Или используйте: `https://api.telegram.org/bot<TOKEN>/getUpdates`

2. Добавьте группу:
   ```
   /addcity Москва
   /addchannel moskva -1001234567890 "Тестовая группа"
   ```
   (Замените `-1001234567890` на реальный Chat ID группы)

### 3. Бот не запущен

Бот должен быть запущен через `/start`.

**Решение:**
1. Отправьте боту: `/start`
2. Проверьте статус: `/status`
3. Убедитесь, что `isRunning: true`

### 4. Группы vs Каналы

**Каналы (channels):**
- Обрабатываются через `channel_post` событие
- Бот должен быть администратором

**Группы (groups/supergroups):**
- Обрабатываются через `message` событие
- Бот должен быть администратором
- Могут обрабатываться иначе

**Проверка:**
- Откройте логи бота
- Отправьте сообщение в группу
- Проверьте, приходит ли событие `message` с правильным `chat.type`

---

## ✅ Пошаговая диагностика:

### Шаг 1: Проверьте, что бот - администратор

1. Откройте группу
2. Настройки → Администраторы
3. Убедитесь, что бот есть в списке

### Шаг 2: Проверьте Chat ID группы

```bash
# Используйте бота @userinfobot
# Или проверьте логи бота при добавлении в группу
```

### Шаг 3: Добавьте группу в базу

```
/addcity Москва
/addchannel moskva -1001234567890 "Тестовая группа"
```

### Шаг 4: Проверьте статус бота

```
/status
```

Должно быть: `isRunning: true`

### Шаг 5: Проверьте логи

Отправьте сообщение в группу и проверьте логи бота:
- Должно появиться: `🔵 handleChannelMessage ВЫЗВАН`
- Или: `📨 Получено сообщение из группы`

---

## 🐛 Если ничего не помогает:

### Проверьте логи бота:

В терминале, где запущен `npm run bot:polling`, должны быть логи:
```
📡 Ожидаю обновления от Telegram...
```

При отправке сообщения в группу должны появиться логи:
```
🔵 handleChannelMessage ВЫЗВАН
   Chat Type: group (или supergroup)
   Chat ID: -1001234567890
```

### Проверьте базу данных:

```bash
npx prisma studio
```

Проверьте таблицу `Channel`:
- Есть ли ваша группа?
- `isActive: true`?
- Правильный `chatId`?

---

## 💡 Альтернатива: Используйте канал вместо группы

Каналы обрабатываются надежнее:
1. Создайте канал в Telegram
2. Добавьте бота как администратора
3. Добавьте канал через `/addchannel`
4. Отправьте сообщение в канал

---

## ✅ Итого:

1. ✅ Бот - администратор группы
2. ✅ Группа добавлена через `/addchannel`
3. ✅ Бот запущен (`/start`)
4. ✅ Проверьте логи при отправке сообщения

