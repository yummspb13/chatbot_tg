# Настройка Webhook для Telegram бота

## 🔴 Проблема: Бот не отвечает на команды

Если команды `/start` и `/listchannels` не отображаются в логах, значит webhook не настроен.

## ✅ Решение: Настроить webhook

### Шаг 1: Узнайте ваш BOT_TOKEN

1. Откройте Vercel Dashboard
2. Перейдите в ваш проект
3. Откройте **Settings** → **Environment Variables**
4. Найдите `TELEGRAM_BOT_TOKEN`
5. Скопируйте значение

**Или** найдите в `.env` файле локально.

### Шаг 2: Узнайте ваш Vercel URL

Ваш URL: `https://chatbot-irhw19y8p-pixelprint.vercel.app`

(Или проверьте в Vercel Dashboard → ваш проект → **Domains**)

### Шаг 3: Настройте webhook

Выполните команду (замените `<BOT_TOKEN>` на ваш токен):

```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
     -d "url=https://chatbot-irhw19y8p-pixelprint.vercel.app/api/tg/webhook"
```

**Пример:**
```bash
curl -X POST "https://api.telegram.org/bot123456789:ABCdefGHIjklMNOpqrsTUVwxyz/setWebhook" \
     -d "url=https://chatbot-irhw19y8p-pixelprint.vercel.app/api/tg/webhook"
```

### Шаг 4: Проверьте webhook

```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```

**Ожидаемый результат:**
```json
{
  "ok": true,
  "result": {
    "url": "https://chatbot-irhw19y8p-pixelprint.vercel.app/api/tg/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### Шаг 5: Проверьте работу бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить

### Шаг 6: Проверьте логи Vercel

1. Откройте Vercel Dashboard
2. Перейдите в ваш проект
3. Откройте **Deployments** → последний деплой → **Logs**
4. Должны быть запросы `POST /api/tg/webhook`
5. Должны быть логи обработки команд

## 🔧 Альтернатива: Использовать polling для разработки

Если webhook не работает, можно использовать polling режим локально:

```bash
npm run bot:polling
```

Но для продакшена нужен webhook.

## ❌ Частые проблемы

### Проблема: "Webhook URL must be HTTPS"

**Решение:** Убедитесь, что URL начинается с `https://`

### Проблема: "Webhook can be set up only on ports 80, 88, 443, 8443"

**Решение:** Не указывайте порт в URL, Vercel автоматически использует 443

### Проблема: "Webhook was set, but Vercel returns 404"

**Решение:** 
1. Проверьте, что файл `app/api/tg/webhook/route.ts` существует
2. Проверьте, что деплой завершился успешно
3. Проверьте URL - должен быть `/api/tg/webhook`, а не `/api/tg/webhook/`

### Проблема: "Webhook работает, но бот не отвечает"

**Решение:**
1. Проверьте логи Vercel - есть ли ошибки?
2. Проверьте, что `TELEGRAM_BOT_TOKEN` установлен в Vercel
3. Проверьте, что бот запущен (отправьте `/start`)

