# 🚀 Деплой на Vercel

## Для продакшена используйте webhook режим

После деплоя на Vercel у вас будет автоматический HTTPS URL, который идеально подходит для webhook.

## Шаги деплоя:

### 1. Подготовка к деплою

```bash
# Убедитесь, что все зависимости установлены
npm install

# Сгенерируйте Prisma Client
npx prisma generate

# Проверьте, что проект собирается
npm run build
```

### 2. Деплой на Vercel

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel

# Или через GitHub:
# 1. Запушьте код в репозиторий
# 2. Подключите репозиторий к Vercel
# 3. Настройте переменные окружения в Vercel Dashboard
```

### 3. Настройка переменных окружения в Vercel

В Vercel Dashboard → Settings → Environment Variables добавьте:

```
TELEGRAM_BOT_TOKEN=...
TELEGRAM_ADMIN_CHAT_ID=...
OPENAI_API_KEY=...
OPENAI_MODEL=gpt-4o-mini
BOT_API_KEY=17d38eb4c014c524e52675ab75661f0dcc8768cbc749a62e7309fabe6c9905cd
AFISHA_DRAFT_URL=https://kiddeo.vercel.app/api/bot/events/draft
DATABASE_URL=postgresql://... (Postgres для продакшена)
```

### 4. Настройка базы данных

Для продакшена используйте Postgres:

```bash
# Обновите DATABASE_URL в Vercel
# Затем выполните миграции:
npx prisma migrate deploy
```

### 5. Настройка webhook после деплоя

После деплоя получите URL вашего приложения (например: `https://your-app.vercel.app`)

```bash
# Установите webhook
npm run webhook:set:prod https://your-app.vercel.app

# Или вручную:
npm run webhook:set https://your-app.vercel.app/api/tg/webhook
```

### 6. Проверка

```bash
# Проверьте статус webhook
npm run webhook:info
```

## Переключение между режимами:

### Polling (локальная разработка):
```bash
npm run bot:polling
```

### Webhook (продакшен):
```bash
# После деплоя на Vercel
npm run webhook:set:prod <vercel-url>
```

### Удаление webhook (вернуться к polling):
```bash
npm run webhook:delete
```

## Важные замечания:

1. **База данных**: Для продакшена используйте Postgres (Vercel Postgres, Supabase, или другой провайдер)
2. **Переменные окружения**: Все секреты должны быть в Vercel Dashboard
3. **Webhook URL**: Vercel автоматически предоставляет HTTPS, идеально для webhook
4. **Мониторинг**: Используйте Vercel Logs для отслеживания ошибок

