# Какая информация извлекается из постов

## 📋 Извлекаемые поля

Из каждого поста в канале извлекается следующая информация:

### ✅ Обязательные поля (без них пост пропускается)

1. **`title`** (название мероприятия)
   - Извлекается из текста поста
   - Если не найдено - пост пропускается

2. **`startDateIso`** (дата и время начала)
   - Формат: ISO 8601 UTC (например, `2025-11-15T15:00:00Z`)
   - Если не найдено - пост пропускается
   - Поддерживает относительные даты ("сегодня", "завтра", "через 3 дня")
   - Использует московский часовой пояс (Europe/Moscow), но сохраняет в UTC

### 📝 Опциональные поля

3. **`endDateIso`** (дата и время окончания)
   - Формат: ISO 8601 UTC
   - Если не указано - остается `null`

4. **`venue`** (место проведения)
   - Адрес или название места
   - Например: "5-й павильон киностудии «Ленфильм»"

5. **`cityName`** (название города)
   - Если не указано в тексте, берется из настроек канала
   - Например: "Санкт-Петербург", "Москва"

6. **`description`** (описание мероприятия)
   - Краткое описание, сжатое из текста поста
   - Если не извлечено AI, используется первые 1000 символов оригинального текста

### 🖼 Медиа

7. **`coverImage`** (обложка)
   - URL первого изображения из поста
   - Если есть несколько изображений, первое становится обложкой

8. **`gallery`** (галерея)
   - Массив URL остальных изображений (если их больше одного)
   - Сохраняется как JSON массив

### 🔗 Метаданные

9. **`sourceLink`** (ссылка на оригинальный пост)
   - Формат: `https://t.me/c/{chatId}/{messageId}`
   - Создается автоматически на основе chatId и messageId

10. **`telegramMessageId`** и **`telegramChatId`**
    - ID сообщения и канала в Telegram
    - Используются для проверки дубликатов

## 🔄 Процесс извлечения

### Шаг 1: Классификация
- Определяется тип сообщения: `EVENT`, `GOING_OUT` или `AD`
- Реклама (`AD`) пропускается автоматически

### Шаг 2: Извлечение полей (AI)
- Используется OpenAI GPT-4o-mini (или другой провайдер)
- AI анализирует текст и извлекает структурированные данные
- **Важно**: AI не выдумывает данные - если информации нет, возвращает `null`

### Шаг 3: Проверка обязательных полей
- Если нет `title` или `startDateIso` - пост пропускается
- Логируется причина пропуска

### Шаг 4: Проверка дубликатов
- Проверяется, не обрабатывался ли уже пост с таким же `title` и `startDate`
- Дубликаты пропускаются

### Шаг 5: Обработка изображений
- Извлекаются все изображения из поста
- Первое изображение → `coverImage`
- Остальные → `gallery`

### Шаг 6: Предсказание агента
- AI оценивает, стоит ли одобрять мероприятие
- Возвращает: `decision` (APPROVED/REJECTED), `confidence`, `reasoning`

## 📊 Пример извлеченных данных

**Исходный пост:**
```
Выставка «Авторский стиль» в киностудии «Ленфильм»

🗓 15 ноября, 15:00-17:00
💸 бесплатно
🏛 ФудПарк Норд, 3 этаж
📍 проспект Просвещения, 19
```

**Извлеченные данные:**
```json
{
  "title": "Выставка «Авторский стиль»",
  "startDateIso": "2025-11-15T15:00:00Z",
  "endDateIso": "2025-11-15T17:00:00Z",
  "venue": "ФудПарк Норд, 3 этаж, проспект Просвещения, 19",
  "cityName": "Санкт-Петербург",
  "description": "Выставка в киностудии «Ленфильм»",
  "coverImage": "https://...",
  "gallery": ["https://...", "https://..."]
}
```

## 🎯 Что сохраняется в базу данных

Все извлеченные поля сохраняются в таблицу `DraftEvent`:

- `title` - название
- `startDate` - дата начала (DateTime)
- `endDate` - дата окончания (DateTime?, опционально)
- `venue` - место (String?, опционально)
- `description` - описание (String?, опционально)
- `cityName` - город (String?, опционально)
- `coverImage` - URL обложки (String?, опционально)
- `gallery` - JSON массив URL изображений (String?, опционально)
- `sourceLink` - ссылка на пост (String?, опционально)
- `telegramMessageId` - ID сообщения
- `telegramChatId` - ID канала
- `status` - статус (NEW, PENDING, APPROVED, REJECTED, SENT_TO_AFISHA, DUPLICATE)

## ⚠️ Ограничения

1. **Обязательные поля**: Без `title` или `startDateIso` пост не обрабатывается
2. **Реклама**: Посты, классифицированные как реклама, пропускаются
3. **Дубликаты**: Посты с одинаковым названием и датой пропускаются
4. **Точность AI**: AI может ошибаться в извлечении данных, поэтому требуется ручная проверка

## 🔍 Где посмотреть извлеченные данные

1. **В логах**: После обработки поста в логах видно все извлеченные поля
2. **В админ-панели**: `/admin/drafts` - список всех черновиков с извлеченными данными
3. **В карточке одобрения**: При отправке карточки в группу показываются все извлеченные поля

