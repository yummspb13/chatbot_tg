# Диагностика проблемы с сохранением в БД

## Проблема
Данные не сохраняются в БД через запросы Prisma.

## Возможные причины

### 1. Проблема с подключением к БД
- Проверьте переменную `DATABASE_URL` в `.env`
- Убедитесь, что БД доступна и не заблокирована
- Проверьте логи подключения в консоли

### 2. Ошибки валидации данных
- Проверьте, что все обязательные поля заполнены
- Убедитесь, что типы данных соответствуют схеме
- Проверьте, что новые поля (`partnerLink`, `isFree`, `tickets`, `conversationId`) добавлены в БД через SQL миграцию

### 3. Ошибки транзакций
- Prisma может откатывать транзакции при ошибках
- Проверьте логи на наличие ошибок валидации

## Что было добавлено для диагностики

1. **Расширенное логирование в `messageHandler.ts`**:
   - Логирование всех данных перед созданием draft
   - Обработка ошибок с детальным выводом
   - Логирование успешного создания

2. **Логирование запросов в `prisma.ts`**:
   - В режиме разработки включены логи всех запросов
   - Обработка ошибок подключения

3. **SQL миграция**:
   - Файл `prisma/migrations/manual_migration.sql` содержит все необходимые SQL команды
   - Выполните их вручную в вашей БД

## Как проверить

1. **Проверьте логи консоли**:
   - Ищите строки с `💾 STEP5: CREATING_DRAFT`
   - Ищите строки с `✅ DRAFT_CREATED` или `❌ ОШИБКА`

2. **Проверьте подключение к БД**:
   ```bash
   npx prisma db pull
   ```
   Если команда выполняется успешно, подключение работает.

3. **Проверьте схему БД**:
   ```sql
   SELECT column_name, data_type, is_nullable
   FROM information_schema.columns
   WHERE table_name = 'afisha_bot_DraftEvent'
   ORDER BY ordinal_position;
   ```
   
   Убедитесь, что есть поля:
   - `partnerLink`
   - `isFree`
   - `tickets`
   - `conversationId`

4. **Проверьте создание draft напрямую**:
   ```typescript
   const testDraft = await prisma.draftEvent.create({
     data: {
       title: 'Test Event',
       startDate: new Date(),
       telegramMessageId: 'test-123',
       telegramChatId: 'test-chat',
     }
   })
   console.log('Test draft created:', testDraft.id)
   ```

## Решение

1. **Выполните SQL миграцию** из `prisma/migrations/manual_migration.sql`

2. **Проверьте логи** при следующей обработке сообщения

3. **Если ошибка повторяется**, скопируйте полный текст ошибки из логов

