# Чеклист проверки продакшена

## 📋 Автоматическая проверка

Запустите скрипт для автоматической проверки всех переменных окружения:

```bash
npm run check:production:env
```

Этот скрипт проверит:
- ✅ Все обязательные переменные окружения
- ✅ Корректность значений (AI_PROVIDER должен быть 'openai', не 'mock')
- ✅ Наличие webhook в Telegram
- ✅ Соответствие webhook URL продакшену

## 🔍 Ручная проверка

### 1. Vercel Dashboard → Settings → Environment Variables

Проверьте наличие и правильность следующих переменных:

#### Обязательные:
- [ ] `AI_PROVIDER=openai` ⚠️ **КРИТИЧНО: должен быть "openai", НЕ "mock"!**
- [ ] `OPENAI_API_KEY=sk-...` (должен начинаться с "sk-")
- [ ] `TELEGRAM_BOT_TOKEN=...` (должен содержать ":")
- [ ] `TELEGRAM_ADMIN_CHAT_ID=120352240`
- [ ] `TELEGRAM_PUBLISH_GROUP_ID=-4993347411`
- [ ] `DATABASE_URL=postgresql://...` (должен быть PostgreSQL, не SQLite)
- [ ] `JWT_SECRET=...` (минимум 32 символа)
- [ ] `ADMIN_PASSWORD_HASH=$2a$10$...` (должен начинаться с "$2a$" или "$2b$")

#### Опциональные (но рекомендуемые):
- [ ] `BOT_API_KEY=...` (для связи с Worker)
- [ ] `WORKER_API_KEY=...` (для Worker)

### 2. Worker (Render.com или другой сервис) → Environment

Проверьте наличие и правильность следующих переменных:

#### Обязательные:
- [ ] `MAIN_APP_URL=https://your-app.vercel.app` ⚠️ **Должен указывать на Vercel URL (без /api/tg/webhook)**
- [ ] `BOT_API_KEY=...` (должен совпадать с Vercel)
- [ ] `TELEGRAM_API_ID=...`
- [ ] `TELEGRAM_API_HASH=...`
- [ ] `TELEGRAM_SESSION_STRING=...` (должен быть длинным, минимум 50 символов)

#### Опциональные:
- [ ] `BOT_WEBHOOK_URL=https://your-app.vercel.app` (если не установлен, используется MAIN_APP_URL)

### 3. Telegram Webhook

Проверьте webhook через команду:

```bash
npm run webhook:check
```

Или вручную:

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

**Ожидаемый результат:**
- ✅ Webhook URL должен быть: `https://your-app.vercel.app/api/tg/webhook`
- ✅ Не должен быть `localhost` или `127.0.0.1`
- ✅ `pending_update_count` должен быть 0 (или небольшим числом)

**Если webhook не установлен или неправильный:**

```bash
npm run webhook:set:prod https://your-app.vercel.app
```

### 4. Проверка работы системы

#### Шаг 1: Проверка webhook endpoint

Откройте в браузере или через curl:

```bash
curl https://your-app.vercel.app/api/tg/webhook
```

**Ожидаемый ответ:**
```json
{
  "status": "ok",
  "environment": "production",
  "aiProvider": "openai",
  "aiProviderStatus": "openai",
  "timestamp": "..."
}
```

⚠️ Если `aiProvider` или `aiProviderStatus` показывает "mock" - это ошибка! Должно быть "openai".

#### Шаг 2: Проверка логов Vercel

1. Откройте Vercel Dashboard → ваш проект → Logs
2. Отправьте тестовое сообщение в канал
3. Проверьте логи - должно быть:
   - `🚀 Webhook endpoint initialized`
   - `AI Provider: OPENAI` (не MOCK!)
   - `📥 WEBHOOK RECEIVED: message`
   - `✅ WEBHOOK: processed`

#### Шаг 3: Проверка логов Worker

1. Откройте Render.com Dashboard (или другой сервис) → ваш Worker → Logs
2. Отправьте тестовое сообщение в канал
3. Проверьте логи - должно быть:
   - `📨 Получено сообщение из канала ...`
   - `🔄 Отправляю сообщение боту на https://your-app.vercel.app/api/tg/webhook...`
   - `✅ Сообщение отправлено боту через webhook`

⚠️ Если видите ошибки типа "fetch failed", "Connection refused" или "Timeout" - проверьте:
- `MAIN_APP_URL` в Worker указывает на правильный Vercel URL
- Vercel приложение работает и доступно

#### Шаг 4: Проверка полного flow

1. Отправьте сообщение в канал (который добавлен в мониторинг)
2. Проверьте логи Worker - сообщение должно быть отправлено в webhook
3. Проверьте логи Vercel - webhook должен получить и обработать сообщение
4. Проверьте группу `-4993347411` - должна прийти карточка с кнопками "Одобрить"/"Отказать"
5. Нажмите "Одобрить" - событие должно быть создано

## 🚨 Частые проблемы

### Проблема 1: AI_PROVIDER установлен как "mock"

**Симптомы:**
- В логах Vercel: `🤖 AI Provider: MOCK`
- События не обрабатываются правильно

**Решение:**
1. Vercel Dashboard → Settings → Environment Variables
2. Найдите `AI_PROVIDER`
3. Измените значение на `openai`
4. Передеплойте проект (Redeploy)

### Проблема 2: Worker не может отправить сообщение в webhook

**Симптомы:**
- В логах Worker: `❌ Ошибка отправки боту: fetch failed`
- В логах Worker: `❌ Ошибка отправки боту: Connection refused`

**Решение:**
1. Проверьте `MAIN_APP_URL` в Worker - должен быть `https://your-app.vercel.app` (без `/api/tg/webhook`)
2. Проверьте, что Vercel приложение работает (откройте URL в браузере)
3. Проверьте, что webhook endpoint доступен: `curl https://your-app.vercel.app/api/tg/webhook`

### Проблема 3: Webhook не установлен

**Симптомы:**
- `npm run webhook:check` показывает "Webhook не установлен"
- Бот не отвечает на команды

**Решение:**
```bash
npm run webhook:set:prod https://your-app.vercel.app
```

### Проблема 4: Карточки не приходят в группу

**Симптомы:**
- Сообщения обрабатываются, но карточки не появляются в группе

**Решение:**
1. Проверьте `TELEGRAM_PUBLISH_GROUP_ID=-4993347411` в Vercel
2. Убедитесь, что бот добавлен в группу и имеет права на отправку сообщений
3. Проверьте логи Vercel на наличие ошибок при отправке

## ✅ Итоговый чеклист

Перед тем как считать продакшен готовым:

- [ ] Все переменные окружения установлены в Vercel
- [ ] Все переменные окружения установлены в Worker
- [ ] `AI_PROVIDER=openai` (не mock!)
- [ ] Webhook установлен и указывает на правильный Vercel URL
- [ ] Webhook endpoint отвечает и показывает правильный AI_PROVIDER
- [ ] Логи Vercel показывают `AI Provider: OPENAI`
- [ ] Worker успешно отправляет сообщения в webhook
- [ ] Тестовое сообщение в канал обрабатывается полностью
- [ ] Карточка приходит в группу
- [ ] Кнопки "Одобрить"/"Отказать" работают

## 📞 Полезные команды

```bash
# Проверка всех переменных окружения
npm run check:production:env

# Проверка webhook
npm run webhook:check

# Установка webhook для продакшена
npm run webhook:set:prod https://your-app.vercel.app

# Получение информации о webhook
npm run webhook:info
```

