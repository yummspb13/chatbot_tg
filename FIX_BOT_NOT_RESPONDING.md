# 🔧 Исправление: Бот не отвечает на /start

## ❌ Проблема:
- Бот не реагирует на команды
- Webhook не установлен

## ✅ Решение:

### 1. Установите webhook на Vercel

**Вариант A: Через скрипт (автоматически)**
```bash
npm run webhook:set:prod
```

**Вариант B: Вручную через curl**
```bash
# Замените YOUR_BOT_TOKEN на ваш токен
curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https://chatbot-irhw19y8p-pixelprint.vercel.app/api/tg/webhook"
```

### 2. Проверьте webhook

```bash
npm run webhook:info
```

**Ожидаемый результат:**
```
URL: https://chatbot-irhw19y8p-pixelprint.vercel.app/api/tg/webhook
Pending updates: 0
```

### 3. Проверьте переменные окружения в Vercel

Убедитесь, что в Vercel Environment Variables установлены:
- ✅ `TELEGRAM_BOT_TOKEN`
- ✅ `TELEGRAM_ADMIN_CHAT_ID`
- ✅ `OPENAI_API_KEY`
- ✅ `DATABASE_URL`
- ✅ `Afisha_API_URL`
- ✅ `Afisha_API_KEY`

### 4. Проверьте логи в Vercel

1. Откройте Vercel Dashboard
2. Выберите проект
3. Перейдите в **"Deployments"** → выберите последний деплой
4. Перейдите в **"Functions"** → `/api/tg/webhook`
5. Проверьте логи на наличие ошибок

### 5. Протестируйте бота

После установки webhook:
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить

---

## 🐛 Типичные проблемы:

### Проблема 1: Webhook установлен, но бот не отвечает

**Причина:** Ошибки в коде или отсутствие переменных окружения

**Решение:**
1. Проверьте логи в Vercel
2. Проверьте переменные окружения
3. Убедитесь, что админ-панель работает

### Проблема 2: "pending_update_count" > 0

**Причина:** Есть необработанные обновления

**Решение:**
1. Установите webhook (обновления обработаются автоматически)
2. Или удалите webhook и используйте polling:
   ```bash
   npm run webhook:delete
   npm run bot:polling
   ```

### Проблема 3: Vercel требует аутентификацию

**Причина:** Deployment Protection включен

**Решение:**
1. Отключите Deployment Protection в Vercel Settings
2. Или добавьте bypass token в URL webhook

---

## ✅ После исправления:

1. ✅ Webhook установлен
2. ✅ Бот отвечает на команды
3. ✅ Логи работают без ошибок

