# ✅ Cron для пробуждения Worker настроен

## Что настроено

- **Сервис:** cron-job.org
- **URL:** `https://chatbot-tg.onrender.com/runner/wake`
- **Метод:** POST ✅
- **Частота:** каждые 5-10 минут

## Как проверить, что cron работает

### 1. Проверьте логи в cron-job.org

1. Откройте [cron-job.org Dashboard](https://cron-job.org)
2. Найдите вашу задачу
3. Перейдите в **Execution History**
4. Проверьте последние выполнения:
   - ✅ **Status:** должен быть `200 OK` или `Success`
   - ✅ **Response:** должен содержать `"success":true`

### 2. Проверьте логи Worker на Render.com

1. Откройте [Render.com Dashboard](https://dashboard.render.com)
2. Найдите ваш Web Service (`chatbot-tg`)
3. Перейдите в **Logs**
4. Каждые 5-10 минут должно появляться:
   ```
   🔔 Запрос на пробуждение Worker...
   ✅ Worker уже работает, пробуждение не требуется
   ```
   Или если Worker был спящим:
   ```
   🔔 Запрос на пробуждение Worker...
   ⚠️ Worker не работает, запускаю заново...
   ✅ Worker пробужден и мониторинг запущен
   ```

### 3. Проверьте статус Worker

```bash
curl https://chatbot-tg.onrender.com/runner/status
```

Должен вернуть:
```json
{
  "isRunning": true,
  "monitoring": {
    "isMonitoring": true,
    "isConnected": true
  }
}
```

## Что происходит теперь

1. **Каждые 5-10 минут** cron-job.org отправляет POST запрос на `/runner/wake`
2. Worker проверяет свой статус
3. Если Worker уснул или мониторинг не активен, он пробуждается автоматически
4. Worker остается активным и может получать сообщения из Telegram

## Преимущества

✅ **Worker не засыпает** - постоянные запросы предотвращают idle timeout  
✅ **Автоматическое восстановление** - если мониторинг упал, он перезапустится  
✅ **Не требует ручного вмешательства** - все работает автоматически  

## Если что-то не работает

### Проблема: Cron не выполняется

**Решение:**
1. Проверьте, что задача **активна** в cron-job.org
2. Проверьте, что указан правильный URL
3. Проверьте, что выбран метод **POST** (не GET!)
4. Проверьте логи выполнения в cron-job.org

### Проблема: Worker все еще засыпает

**Решение:**
1. Уменьшите интервал до 5 минут (вместо 10)
2. Проверьте, что cron действительно выполняется (смотрите Execution History)
3. Проверьте логи Worker - должны быть запросы каждые 5-10 минут

### Проблема: Ошибка в логах cron

**Решение:**
1. Проверьте URL - должен быть `https://chatbot-tg.onrender.com/runner/wake`
2. Проверьте метод - должен быть **POST**
3. Проверьте, что Worker доступен: `curl https://chatbot-tg.onrender.com/health`

## Дополнительные настройки (опционально)

### Уведомления в cron-job.org

Можно настроить уведомления на email, если cron не выполняется:
1. В cron-job.org → ваша задача → **Notifications**
2. Включите уведомления об ошибках

### Мониторинг через Vercel endpoint (после деплоя)

После того как `/api/worker/wake` задеплоится на Vercel, можно переключиться на него:
- **URL:** `https://chatbot-tg.vercel.app/api/worker/wake`
- **Метод:** GET или POST (оба работают)
- **Преимущество:** Vercel endpoint может делать дополнительную проверку перед пробуждением

## Готово! 🎉

Теперь Worker будет автоматически пробуждаться каждые 5-10 минут и не будет засыпать.

Проверьте через несколько минут:
1. Логи Worker на Render.com - должны быть запросы от cron
2. Статус Worker - должен быть активным
3. Отправьте тестовое сообщение в канал - должно обработаться

